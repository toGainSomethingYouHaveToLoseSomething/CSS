content - padding - border - margin
height and width is for content

border-width:2px;
border-style: solid/ dotted / dashed ;
border-color : balck;
shorthand -> border: 2px solid balck;
border-radios: 50px or 50%

padding->
padding-left:
padding-right:
padding-top:
padding-bottom:
shorthand-> padding: top right bottom left; clock-wise

margin: structure is same as paddings


Display Property->

Display: inline / block / inline-block / none

inline- takes only the space required by the element. (no margin/padding)
block- takes full space available in width.
inline-block- Similar to inline but we can set margin and padding
none - To remove element from document flow.

visibility: hidden; (space will be reserved)

Alpha Channel: Opacity(0-1) RGBA color:rgb(255, 0, 0,0.5);


relative units-> (%, em, rem)
% -> relative to parent element

Position Property->
Position: static / relative / absolute / fixed

static - default position (the top,right,bottom,left and z-index properties have no effect)
relative - element is relative to itself. (the top,right,bottom,left,and z-index will work)
absolute - positioned relative to its closest positioned ancestor. (removed from flow)
fixed - positioned relative to browser. (removed from flow) (canbe used for navbar)
sticky - positioned based on user's scroll position

top:10px;
z-index: 1/2/-1/-2/0

background-image->

background-image: url("PhoroURL");
background-size: cover/ contain /auto

cover - no empty space in the area
contain - full image (repeated in empty space) ; background-repeat: no-repeat;
auto - default